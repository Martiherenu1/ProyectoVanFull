# Vanfull backend — dependencias
# API
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Config
pydantic>=2.7
pydantic-settings>=2.3

# Base de datos (async)
sqlalchemy>=2.0
asyncpg>=0.29
alembic>=1.13

# Seguridad / auth (RF-045) — se usará al implementar login
passlib[bcrypt]>=1.7
python-jose[cryptography]>=3.3

# Integraciones y agente AG-01
httpx>=0.27

# Testing y calidad de código (dev)
pytest>=8
pytest-asyncio>=0.23
ruff>=0.6
